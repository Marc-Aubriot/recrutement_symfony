{# templates/default/getmesannonces.twig #}
{% extends 'utilisateur/recruteur.twig' %}

{% block sectionMain %}
    
    <div class="candidatureListBox row">
        <div>
            <h3>Liste de mes annonces.</h3>

            {% for item in annonces %}
                <div class="row m-1 bg-info rounded">
                    <div class="col-9">
                        <p><b>{{ item.intitulé|e }} {{ item.horaires|e }} {{ item.salaire|e }}</b></p>
                    </div>

                    <div class="col-3">
                        <a href={{ url('voirlescandidats', {'itemid': item.id, 'id':user.id}) }}>Voir les candidats</a>
                    </div>
                    
                </div>
            {% endfor %}

        </div>
        
    </div>

{% endblock %}